import styled from "styled-components";
import {StyledText} from "./layout/text.styled";
import {gapsRow} from "../styles/utils/gaps";

export const StyledExternalLink = styled(StyledText)`
    ${gapsRow("0.25rem")}
`
