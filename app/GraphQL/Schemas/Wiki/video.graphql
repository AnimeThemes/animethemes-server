enum VideoColumnsOrderable {
    ID @enum(value: "video_id")
    FILENAME
    BASENAME
    LYRICS
    NC
    OVERLAP
    RESOLUTION
    SIZE
    SOURCE
    SUBBED
    TAGS
    UNCEN
    CREATED_AT
    UPDATED_AT
    DELETED_AT
}